# 徒步助手 · 开发路线图（ROADMAP）

> 创建：2026-08-28 · 由 DeepSeek 接手维护 · 与用户确认后逐项执行
> 每项任务含验收标准，完成即打勾 ✅

---

## P0 — 收尾在途工作（当前未提交的密码重置功能）

| # | 任务 | 验收标准 | 预估 |
|---|------|----------|------|
| P0-1 | 启动前后端，跑通 `verify-password-reset.mjs` E2E 脚本（忘记密码→重置→新密码登录→改密） | E2E 脚本全部断言通过 | 30 分钟 |
| P0-2 | 提交代码：backend `auth.py`/`schemas/user.py` + frontend `ForgotPassword`/`ResetPassword`/`Login`/`Settings`/`App.tsx` + E2E 脚本 + CLAUDE.md 更新 | 一个干净的 commit，消息符合规范 | 15 分钟 |
| P0-3 | push 到 origin（当前 main 领先 origin 2 个提交） | `git status` 干净，远程同步 | 10 分钟 |

**P0 完成后：密码重置/修改密码功能正式入库，工作区清零。**

---

## P1 — 数据与体验补全（真实用户可用性）

| # | 任务 | 验收标准 | 预估 |
|---|------|----------|------|
| P1-1 | ✅ **装备预设模板修复与增强**（完成 2026-08-28）：① 分类迁移脚本 `migrate_gear_categories.py`（旧分类 炊具系统/急救/饮水/食物 → 对齐代码 13 分类，幂等可重复执行）；② 前端模板补真实重量（53 件，总重 19.3kg），一键填充全量写入 | gear_categories 与代码 DEFAULT_CATEGORIES 完全一致；一键填充覆盖 13/13 分类、53 件、重量全部非零（`verify_gear_presets.py` 全绿）；**backpacks 延后**（模型存在但无管理 UI/API 端点，需先做背包管理功能） | 2~3 小时 ✅ |
| P1-2 | ✅ **路线分段数据质量抽检**（完成 2026-08-28）：新增 `audit_route_segments.py` 全量审计（字段完整性/数值合理性/分段 vs KB 偏差/天数一致），修复 5 处：雨崩重复键（静默覆盖）、崆峒山 day 编号错误、独库公路元数据偏差 38%、四姑娘山二峰天数不符、白云山审计误报；报告见 `docs/route-audit-report.md` | 136 条中 135 条审计通过；仅剩太白山 Day1 2990m 判定为数据正确（数学自洽）；遗留建议：广州白云山/白云山疑似重复条目待用户决策 | 3~4 小时 ✅ |
| P1-3 | 清理测试数据：`routes` 表 3 条 test 路线、DB 中明显测试账号（保留策略与用户确认） | 生产数据库无 test 痕迹 | 30 分钟 |
| P1-4 | 生产模式密码重置 SMTP 接入（当前代码 `TODO 接入 SMTP`）：配置化邮件发送 + 失败降级提示 | 生产配置下发真实邮件；无 SMTP 时给出明确错误而非静默"已发送" | 2 小时 |

**P1 完成后：新用户体验完整 —— 有装备数据、路线数据可信、无测试脏数据。**

---

## P2 — 质量保障（防回归）

| # | 任务 | 验收标准 | 预估 |
|---|------|----------|------|
| P2-1 | 后端 pytest 单元测试：auth（注册/登录/重置）、plans（工作流状态机）、equipment（CRUD）、meal（预算分层）、weather（mock 外部 API） | 核心模块覆盖率 ≥ 60%，`pytest` 全绿 | 4~6 小时 |
| P2-2 | 前端关键流程自动化：复用 `verify-password-reset.mjs` 模式，补登录→新建规划→历史列表→记录创建 主路径 E2E | 主路径脚本一键通过 | 3~4 小时 |
| P2-3 | 一键质量脚本：`lint + typecheck + build + pytest` 串联（npm script + 根目录脚本），可选 GitHub Actions CI | 一条命令全绿 | 1 小时 |
| P2-4 | 文档补全：README 项目结构章节补全、docker-compose 实际启动验证、部署说明 | 按 README 可从零启动前后端 + Docker | 1~2 小时 |

**P2 完成后：项目具备可回归的测试基线，后续改动能放心。**

---

## P3 — 新功能方向（CLAUDE.md 下一步 + 探索）

| # | 任务 | 验收标准 | 预估 |
|---|------|----------|------|
| P3-1 | 真实用户测试：真人走完整流程（规划→装备→历史→记录→传照片），收集反馈形成问题清单 | 反馈清单产出，高优先级问题进入修复队列 | 半天~1 天 |
| P3-2 | 社区分享：方案/路线生成可分享链接（只读视图） | 分享链接无需登录可看方案 | 3~4 小时 |
| P3-3 | 离线地图增强：Leaflet 瓦片缓存策略 / PWA 离线增强（当前 SW 已有基础） | 弱网/离线时路线地图可用 | 3~4 小时 |
| P3-4 | 路线覆盖补充：采集失败清单（15 条：终南山/翠华山/祁连山/罗布泊等）二次采集或人工录入 | 知识库 136 → 145+ 条 | 2 小时 |
| P3-5 | 背包管理功能：Backpack 模型已有但无 UI/API，需先做背包 CRUD + 预设背包方案（轻装/重装/冬季），再填充 backpacks 表 | 背包页面可增删改查，预设方案一键创建 | 3~4 小时 |

**P3 完成后：项目具备分享裂变能力 + 离线可用性 + 更广路线覆盖。**

---

## 执行规则

1. 严格按 P0 → P1 → P2 → P3 顺序执行，除非用户调整。
2. 每完成一项：跑对应验证 → 更新本文件打勾 → 提交 commit（功能粒度）。
3. 每轮结束向用户汇报进度与下一步。
4. 需要用户决策的点（如测试数据清理策略）提前询问，不擅自删除用户数据。

## 已知事实备注（接手时核实）

- 知识库：`route_analyst.py` 内置 136 条 + `route_segments.json` 外部分段 136 条 ✅ 已合并
- `collected_routes.json`（72 条）与 `xinjiang_routes.json`（23 条）**已全部导入**知识库（0 缺失）✅
- DB 现状：8 用户 / 61 方案（57 完成）/ 274 分段 / 649 Agent 日志 / 13 装备分类 / 2 装备条目 / 0 背包
- Git：3 个 commit，main 领先 origin 2 个，密码重置功能未提交（P0 处理）
