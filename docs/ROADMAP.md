# 徒步助手 · 开发路线图（ROADMAP）

> 创建：2026-08-28 · 由 DeepSeek 接手维护 · 与用户确认后逐项执行
> 每项任务含验收标准，完成即打勾 ✅

---

## P0 — 收尾在途工作（当前未提交的密码重置功能）

| # | 任务 | 验收标准 | 预估 |
|---|------|----------|------|
| P0-1 | 启动前后端，跑通 `verify-password-reset.mjs` E2E 脚本（忘记密码→重置→新密码登录→改密） | E2E 脚本全部断言通过 | 30 分钟 |
| P0-2 | 提交代码：backend `auth.py`/`schemas/user.py` + frontend `ForgotPassword`/`ResetPassword`/`Login`/`Settings`/`App.tsx` + E2E 脚本 + CLAUDE.md 更新 | 一个干净的 commit，消息符合规范 | 15 分钟 |
| P0-3 | ✅ **push 到 origin**（完成 2026-09-01）：FlClash 代理就绪后一次推送成功，`0d94fab..d07c084` 共 18 个提交全部同步 | `git status` 干净，main 与 origin/main 同步 | 10 分钟 ✅ |

**P0 完成后：密码重置/修改密码功能正式入库，工作区清零。**

---

## P1 — 数据与体验补全（真实用户可用性）

| # | 任务 | 验收标准 | 预估 |
|---|------|----------|------|
| P1-1 | ✅ **装备预设模板修复与增强**（完成 2026-08-28）：① 分类迁移脚本 `migrate_gear_categories.py`（旧分类 炊具系统/急救/饮水/食物 → 对齐代码 13 分类，幂等可重复执行）；② 前端模板补真实重量（53 件，总重 19.3kg），一键填充全量写入 | gear_categories 与代码 DEFAULT_CATEGORIES 完全一致；一键填充覆盖 13/13 分类、53 件、重量全部非零（`verify_gear_presets.py` 全绿）；**backpacks 延后**（模型存在但无管理 UI/API 端点，需先做背包管理功能） | 2~3 小时 ✅ |
| P1-2 | ✅ **路线分段数据质量抽检**（完成 2026-08-28）：新增 `audit_route_segments.py` 全量审计（字段完整性/数值合理性/分段 vs KB 偏差/天数一致），修复 5 处：雨崩重复键（静默覆盖）、崆峒山 day 编号错误、独库公路元数据偏差 38%、四姑娘山二峰天数不符、白云山审计误报；报告见 `docs/route-audit-report.md` | 136 条中 135 条审计通过；仅剩太白山 Day1 2990m 判定为数据正确（数学自洽）；"白云山/广州白云山"重复条目经用户确认已合并（保留广州白云山） | 3~4 小时 ✅ |
| P1-3 | ✅ **清理测试数据**（完成 2026-08-28，用户确认温和清理）：`cleanup_test_data.py` 删除 3 条 test 路线 + 'test trip' + 2 件'新装备'占位装备；保留全部用户与方案历史 | routes 3→0、trip_records 1→0、gear_items 2→0；用户/方案/日志不动 | 30 分钟 ✅ |
| P1-3 | 清理测试数据：`routes` 表 3 条 test 路线、DB 中明显测试账号（保留策略与用户确认） | 生产数据库无 test 痕迹 | 30 分钟 |
| P1-4 | ✅ **生产模式密码重置 SMTP 接入**（完成 2026-08-28）：新增 `email_service.py`（标准库 smtplib，STARTTLS/SSL 双模式，中文邮件+30分钟重置链接）；config 新增 SMTP 配置与 frontend_url；生产分支无 SMTP 配置时明确报 500，发送失败报 503，绝不静默"已发送" | 开发模式回归 E2E 通过；无 SMTP 时 forgot-password 明确报错（已测）；邮件内容/链接构建正确（已测）；.env.example 补充配置说明 | 2 小时 ✅ |

**P1 完成后：新用户体验完整 —— 有装备数据、路线数据可信、无测试脏数据。**

---

## P2 — 质量保障（防回归）

| # | 任务 | 验收标准 | 预估 |
|---|------|----------|------|
| P2-1 | ✅ **后端 pytest 单测**（完成 2026-08-28）：48 个测试覆盖 auth/equipment/plans/workflow/meal/weather/agents 规则引擎，**总覆盖率 71%**（目标 ≥60%）；期间修复 2 个真实 Bug：① equipment API 无效分类导致 500 IntegrityError → 改为 400 校验；② synthesizer `elif not has_detail` 分支引用未定义变量（UnboundLocalError，未知路线时工作流崩溃）→ 重写为纯规则降级；dev 依赖见 `requirements-dev.txt` | `pytest` 全绿（48 passed），`--cov=app` 覆盖率 71%；工作流冒烟测试覆盖 94% | 4~6 小时 ✅ |
| P2-2 | ✅ **前端主路径 E2E**（完成 2026-08-28）：新增 `verify-main-flow.mjs`（Chrome headless 390px）：登录→新建规划（**真实 LLM 工作流** 17s 完成）→历史列表→创建徒步记录，全链路通过；期间修复 3 个真实缺陷：① trips API media 懒加载导致 MissingGreenlet → **一次列表请求即可拖垮整个后端**（list/get 补 selectinload(media)）；② TripDetail useEffect 缺依赖（lint）；③ E2E 路由笔误 /plans/history（实际路由是 /plans）；新增 `test_trips.py` 防回归 | 主路径 E2E 一键通过（约 2-3 分钟）；截图存 frontend/scripts/mainflow-*.png | 3~4 小时 ✅ |
| P2-3 | ✅ **一键质量脚本**（完成 2026-08-28）：根目录 `quality-check.ps1` 串联前端 lint+tsc+build 与后端 pytest（含覆盖率）；frontend 新增 `npm run check` | `npm run check` 全绿（lint 0 警告、tsc、build 1.6s）；`pytest` 48+ 全绿 | 1 小时 ✅ |
| P2-4 | ✅ **文档与部署补全**（完成 2026-08-28）：README 项目结构补全（tests/scripts/ROADMAP/质量工具）、新增"测试与质量"与"部署说明"章节（开发/生产模式，含 SMTP 配置）；docker-compose.yml 经 YAML 语法校验有效，定位澄清为可选生产依赖 | 按 README 可从零启动前后端；**注**：本机未装 Docker，compose 实机启动验证待有 Docker 环境后补做 | 1~2 小时 ✅ |

**P2 完成后：项目具备可回归的测试基线，后续改动能放心。**

---

## P3 — 新功能方向（CLAUDE.md 下一步 + 探索）

| # | 任务 | 验收标准 | 预估 |
|---|------|----------|------|
| P3-1 | 🟡 **真实用户测试**（准备完成，待用户实测 2026-09-01）：测试清单 `docs/USER-TEST-CHECKLIST.md`（9 大环节 30+ 检查点 + 反馈格式）；demo 账号已预置完整数据（真实 AI 生成的 7 区块方案/53 件装备/2 套背包方案/徒步记录），`seed_demo_data.py` 可一键重建 | 用户按清单走完流程，反馈问题清单产出并修复 | 半天~1 天 |
| P3-2 | ✅ **社区分享**（完成 2026-09-01）：方案一键生成免登录分享链接 —— 后端 `share` API（生成/幂等复用/免登录只读读取/所有者撤销，24 字节随机 token）；前端 PlanDetail 分享按钮 + 复制链接 + 公开分享页 `/share/plans/:token`（只读 7 大区块） | `pytest` 59 全绿；CDP 实测：无登录态访问分享链接正常渲染方案（标题/区块/横幅），无重定向、390px 无溢出 | 3~4 小时 ✅ |
| P3-3 | ✅ **离线地图增强**（完成 2026-09-01）：sw.js 增加① OSM 瓦片有界缓存（cache-first，1500 张上限自动淘汰最旧 10%）；② 路线 API 网络优先+离线兜底缓存；③ 派生缓存随 CACHE_NAME 版本自动轮换（bump-sw 兼容） | 离线 E2E 实测通过：在线加载路线地图（4 瓦片）→ 模拟离线重载 → 路线数据与瓦片均从缓存正常渲染，无错误 | 3~4 小时 ✅ |
| P3-4 | ✅ **补充采集失败路线**（完成 2026-09-01）：16 条（终南山/翠华山/祁连山/马蹄寺/茶卡盐湖/雪乡穿越/武隆天坑/黄果树/龙虎山/香山/孟克德古道/喀拉峻/托乎拉苏/罗布泊/阿尔金山/昆仑山），KB + 分段文件同步，审计全通过 | 知识库 135 → **151 条**，分段文件 151 条完全对应，审计 150/151（仅太白山已知判定），运行时匹配验证通过 | 2 小时 ✅ |
| P3-5 | ✅ **背包管理功能**（完成 2026-09-01）：后端 `backpacks` API（CRUD + 4 套预设方案[轻装单日/标准周末/重装长线/冬季雪山]一键创建并自动挂载装备 + 装备挂载/解绑，含权限隔离）；前端新增"背包方案"页面（预设卡片/自定义创建/装备分配下拉/重量统计，390px 无溢出）+ 导航入口；`test_backpacks.py` 6 用例 | `pytest` 全绿；页面 CDP 验证通过（渲染/预设按钮/无溢出） | 3~4 小时 ✅ |

**P3 完成后：项目具备分享裂变能力 + 离线可用性 + 更广路线覆盖。**

---

## 执行规则

1. 严格按 P0 → P1 → P2 → P3 顺序执行，除非用户调整。
2. 每完成一项：跑对应验证 → 更新本文件打勾 → 提交 commit（功能粒度）。
3. 每轮结束向用户汇报进度与下一步。
4. 需要用户决策的点（如测试数据清理策略）提前询问，不擅自删除用户数据。

## 已知事实备注（接手时核实）

- 知识库：`route_analyst.py` 内置 136 条 + `route_segments.json` 外部分段 136 条 ✅ 已合并
- `collected_routes.json`（72 条）与 `xinjiang_routes.json`（23 条）**已全部导入**知识库（0 缺失）✅
- DB 现状：8 用户 / 61 方案（57 完成）/ 274 分段 / 649 Agent 日志 / 13 装备分类 / 2 装备条目 / 0 背包
- Git：3 个 commit，main 领先 origin 2 个，密码重置功能未提交（P0 处理）
